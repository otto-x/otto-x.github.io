<div class="mt2 p0">
	<span class="btn button-narrow">Tags</span>
	<svg class="icon gray" data-icon="chevron-right"></svg>
	<span class="btn button-narrow"><%= page.tag %></span>
</div>
<% page.posts.each(function(single){ %>
	<%- partial('_partial/article', {item: single, hideNav: true, noPadding: true}) %>
<% }) %>
<%- partial('_partial/pagination', {base: url_for(page.base), show: 2}) %>
